import React, {useEffect} from 'react'
import { useDispatch, useSelector } from 'react-redux'
import { getForex } from '../../../features/stock/stockSlice'


export default function Forex(){
    // const selector = useSelector(state => state.stock.forex)
    // useEffect(() => {
      
    // }, [])
    
    return{
        // {selector && selector.map(item => <div key={item.id}>
        //     <div>

        //     </div>
        // </div>
        // )}
    }
}